/**
 * 默认配置
 */

const DEFAULT_MODEL = 'claude-sonnet-4-6';
const DEFAULT_REGIME = 'ming';
const DEFAULT_MAX_TURNS = 50;
const DEFAULT_MAX_BUDGET_USD = 5.0;

module.exports = {
  DEFAULT_MODEL,
  DEFAULT_REGIME,
  DEFAULT_MAX_TURNS,
  DEFAULT_MAX_BUDGET_USD
};
